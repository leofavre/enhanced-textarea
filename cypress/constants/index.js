export const LOREM =
  'And One\n' +
  'Two\n' +
  'Three\n' +
  'Four';

export const DELETE_ALL = '{backspace}'.repeat(LOREM.length);

export const TYPE_DELAY = 0;

export const TYPE_OPTIONS = { delay: TYPE_DELAY };
